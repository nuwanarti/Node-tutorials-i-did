var path = require("path"); // path module to work with file paths

console.log(`Rock on World from ${path.basename(__filename)}`); // __filename is a globally available variable in node and __dirname is same is this
